schema_version = "1.0.0"

id = "btp"
version = "0.5.0"
name = "Blender Texture Protocol"
tagline = "外部贴图编辑器同步纹理 (localhost HTTP)"
maintainer = "fangzhangmnm <mnmfangzhang@gmail.com>"
type = "add-on"

blender_version_min = "4.2.0"

license = [
  "SPDX:MIT",
]

tags = ["Development", "Pipeline"]

# No third-party wheels: pure stdlib (http.server). The WebRTC transport that
# needed aiortc/av/cryptography/… was removed in addon 0.5.0.
